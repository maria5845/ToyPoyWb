<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.toypoy.toypoywb.dao.main.MainDao">

    <select id="selectTest" resultType="String">
        SELECT now()
        FROM dual
    </select>

</mapper>